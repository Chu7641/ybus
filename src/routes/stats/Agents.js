import React from 'react';
import PropTypes from 'prop-types';

Agents.propTypes = {
    
};

function Agents(props) {
    return (
        <div>
            Agents
        </div>
    );
}

export default Agents;