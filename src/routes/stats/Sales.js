import React from 'react';
import PropTypes from 'prop-types';

Sales.propTypes = {
    
};

function Sales(props) {
    return (
        <div>
            Sales
        </div>
    );
}

export default Sales;